utilisateurs
id | pseudo |

listes
| id | user_id (FK vers utilisateurs.id) | nom |

paliers
| id | id_liste (FK vers listes.id) | ordre | nom |

taches
| id | id_liste (FK vers listes.id) | description | terminee | id_palier (FK vers paliers.id) |
